<form method="post" action="{{ path('app_bibliotecario_delete', {'id': bibliotecario.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bibliotecario.id) }}">
    <button class="btn">Delete</button>
</form>
