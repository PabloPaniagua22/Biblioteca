<form method="post" action="{{ path('app_multa_delete', {'id': multum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ multum.id) }}">
    <button class="btn">Delete</button>
</form>
